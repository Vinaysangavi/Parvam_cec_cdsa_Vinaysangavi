#include<stdio.h>// standard script
int main()// standard script
{ int num;
    printf("enter any number:");
    scanf("%d",&num);
    (num%2==0) ? printf("%d is even",num):printf("%d is odd", num);
    

    
}